apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-web-deployment
  labels:
    app: my-k8s-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      project: my-project
  template:
    metadata:
      labels:
        project: my-project
    spec:
      containers:
        - name: my-project-web
          image: nginx:latest
          ports:
            - containerPort: 80
