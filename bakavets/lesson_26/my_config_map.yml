apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-cm
data:
  # propery-like keys; each key maps to a simple value
  interval: "5"
  count: "3"
  # file-like keys
  properties: |
    Hello from World!
    This is demo config!
    As an example.
  config.ini: "this is demo config!"
