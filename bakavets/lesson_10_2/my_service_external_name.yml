apiVersion: v1
kind: Service
metadata:
  name: my-service-external-name
spec:
  # selector: 
    # app: http-server
  # ports:
    # - protocol: TCP
      # port: 80
      # targetPort: 80
  type: ExternalName
  externalName: example.com
